# Runtime Contracts — Source of Truth

Any data structure used at runtime, storage, or transport MUST be documented in this folder before being implemented.

All contracts are explicitly versioned using `schema_version`.
Modifying a STABLE contract is a breaking change.

The engine orchestrates.
Plugins think.
Formats rule everything.
